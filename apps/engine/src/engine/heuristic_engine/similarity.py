"""
Heuristic Engine — similarity.py (Day 3 implementation).

Computes cosine similarity between the current reasoning attempt (N)
and the attempt from window steps ago (N-2 by default).

Formula (from the B.Tech synopsis):
    Sim(N, N−2) = (e_N · e_(N−2)) / (||e_N|| × ||e_(N−2)||)

Stub for Day 1 — full implementation in Day 3.
"""

# TODO (Day 3): Implement compute_cosine_similarity(vec_n, vec_n_minus_2) -> float
#   using numpy dot product, no external libraries beyond numpy.
